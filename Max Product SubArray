largestProduct = arr[0]

max = arr[0]
min = arr[0]

for() //iterate element
{
    //if elem < 0 then swap min and max
    
    //update min and max (elem, max/min * elem)
    
    //update largestProduct

}
